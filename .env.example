# GitHub Configuration
# Get your token from: https://github.com/settings/tokens
# Required scopes: repo (for private repos), public_repo (for public repos only)
GITHUB_TOKEN=your_github_token_here

# Anthropic Claude API Configuration
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Optional: Default repository to analyze (format: owner/repo)
DEFAULT_REPO=owner/repo

# Optional: Claude model to use (default: claude-sonnet-4-20250514)
CLAUDE_MODEL=claude-sonnet-4-20250514

# Optional: Maximum issues to fetch per request (default: 30)
MAX_ISSUES=30

# Optional: Cache TTL in seconds (default: 3600)
CACHE_TTL=3600

# Optional: Output directory for reports (default: ./output)
OUTPUT_DIR=./output

# Optional: Enable debug mode (default: false)
DEBUG=false
